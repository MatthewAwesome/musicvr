export const disconnectAudioParamInputConnections = (audioGraph, audioParam, disconnectAudioNodeInputConnections) => {
    const audioParamConnections = audioGraph.params.get(audioParam);
    if (audioParamConnections !== undefined) {
        audioParamConnections
            .inputs
            .forEach(([source, output]) => {
            source.disconnect(audioParam, output);
            disconnectAudioNodeInputConnections(audioGraph, source);
        });
    }
};
//# sourceMappingURL=/build/es2018/helpers/disconnect-audio-param-input-connections.js.map